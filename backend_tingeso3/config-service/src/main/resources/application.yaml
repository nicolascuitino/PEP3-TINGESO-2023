server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/nicolascuitino/PEP3-TINGESO-2023.git
          search-paths: config-data
  application:
    name: config-service
